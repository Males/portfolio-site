/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses CSS-based configuration
  // Most configuration is done in src/index.css using @theme
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
}
